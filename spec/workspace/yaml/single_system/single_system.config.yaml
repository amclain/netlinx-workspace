systems:
  -
    name: MyClient Conference Room
    id: 3
    description: System description.
    connection: 192.168.1.2
    files: # Automatically glob these files from project root if not specified.
      - include/**/*.axi
      - ir/**/*.irl
      - touch_panel/**/*.TP4
      - touch_panel/**/*.TP5
    excluded_files:
      - include/excluded_file.axi
